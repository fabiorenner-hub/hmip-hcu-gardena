// Mirror of src/version.js for the browser. Kept in sync on every release
// (design-spec §7). The server also reports the live version via /api/state;
// this constant is the build-time fallback used before the first fetch.
window.APP_VERSION = '1.2.0';
window.GITHUB_URL = 'https://github.com/fabiorenner-hub/hmip-hcu-gardena';
